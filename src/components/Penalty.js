import React from 'react';
import { Grid } from '../elements';

const Penalty = (props) => {
    return(
        <React.Fragment>
            <Grid padding="16px">
                <h1>늦은 친구가 받을 벌칙이 필요한가요?</h1>
            </Grid>
        </React.Fragment>
    )
}

export default Penalty;